metadata:
  root: "{{root}}"
datasources:
  - name: "adam"
    backend:
        type: "connector::connector_fs"
        path: "{metadata.root}/data/adam"
  - name: "sdtm"
    backend:
        type: "connector::connector_fs"
        path: "{metadata.root}/data/sdtm"
  - name: "metadata"
    backend:
        type: "connector::connector_fs"
        path: "{metadata.root}/data/metadata"
