# Incomplete within-domain ADaM spec fails when checks disabled

    Specification validation errors found:
    
    [Missing dependencies]
    i Missing columns: ADLB.VISITNUM
    i Required by: ADLB.VISITNUM2 via '_col_mutate.mustache'
    
    Suggestions:
    * Add missing columns to their respective domain specifications
    * Remove dependencies on these columns if they're not needed
    * Check for typos and casing in column or domain names
    * Ensure all required domains are defined in your specifications

# Incomplete within-domain ADaM spec fails when checks enabled

    Specification validation errors found:
    
    [Missing dependencies]
    i Missing columns: ADLB.VISITNUM
    i Required by: ADLB.VISITNUM2 via '_col_mutate.mustache'
    
    Suggestions:
    * Add missing columns to their respective domain specifications
    * Remove dependencies on these columns if they're not needed
    * Check for typos and casing in column or domain names
    * Ensure all required domains are defined in your specifications

