# Complex test w/ missing dm_vaccine.SEX

    Can't select columns that don't exist.
    x Column `SEX` doesn't exist.

---

    Can't select columns that don't exist.
    x Column `SEX` doesn't exist.

---

    Can't select columns that don't exist.
    x Column `SEX` doesn't exist.

# Complex test w/ missing dm_vaccine.AGE and dm.SEX

    Can't select columns that don't exist.
    x Column `SEX` doesn't exist.

---

    Can't select columns that don't exist.
    x Column `SEX` doesn't exist.

---

    i In argument: `NEWFL03 = ifelse(NEWFL01 == 1 & NEWFL02 == 1, 1, 0)`.
    Caused by error:
    ! object 'NEWFL01' not found

# Complex test w/ missing lb.LBSTRESN

    Can't select columns that don't exist.
    x Column `LBSTRESN` doesn't exist.

---

    Can't select columns that don't exist.
    x Column `AVAL` doesn't exist.

