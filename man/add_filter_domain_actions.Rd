% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_filter_domain_actions.R
\name{add_filter_domain_actions}
\alias{add_filter_domain_actions}
\title{Add Filter Domain Actions}
\usage{
add_filter_domain_actions(actions, ui_init, domain_keys)
}
\arguments{
\item{actions}{Data table containing existing action definitions with columns
including node_id, type, domain, outputs, and depend_cols.}

\item{ui_init}{List containing UI initialization data with filter dependencies
and domain filter specifications for each domain.}

\item{domain_keys}{Named list mapping domain names (uppercase) to their
respective key columns required for joining operations.}
}
\value{
Data table combining the original actions with newly created filter_domain
actions, maintaining the same structure and column definitions.
}
\description{
Creates and adds filter_domain actions to the existing actions data table
based on UI initialization data and domain filtering requirements.
}
\details{
This function processes domains that require filtering by:
\enumerate{
\item Identifying domains with filter dependencies from UI initialization data
\item Extracting external domain dependencies and validating their keys
\item Creating comprehensive dependency lists including required join keys
\item Generating filter_domain actions with appropriate inputs and outputs
\item Handling both column copies and computed columns as filter outputs
}

The function ensures that all external domain keys are available and creates
proper dependency chains for filtering operations across multiple domains.

Throws error if external domains referenced in filter dependencies are not found
in the domain_keys lookup table.
}
